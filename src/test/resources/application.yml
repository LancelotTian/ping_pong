# Server settings
server:
  port: 8080
  tomcat:
    max-threads: 2000
    uri-encoding: UTF-8
management:
  port: 0
  security:
    enabled: false

# SPRING PROFILES
spring:
  application:
    name: ping-pong
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true